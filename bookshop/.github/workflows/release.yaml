name: Release

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read
  deployments: write

jobs:
  tests:
     uses: <org/repo>/.github/workflows/test.yaml@main
     secrets: inherit
